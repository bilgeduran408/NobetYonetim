<!--
@model NobetYonetim.Models.Admin

@{
    ViewData["Title"] = "Admin Sil";
}

<h1>@ViewData["Title"]</h1>

<p>Bu admini silmek istediğinizden emin misiniz?</p>
<div>
    <h4>@Model.FullName</h4>
    <p>Email: @Model.Email</p>
    <p>Bölüm: @Model.Departments</p>
</div>

<form asp-action="Delete" method="post">
    <input type="hidden" name="id" value="@Model.AdminId" />
    <button type="submit" class="btn btn-danger">Evet, Sil</button>
    <a href="@Url.Action("Index", "Admin")" class="btn btn-secondary">İptal</a>
</form> 
-->
